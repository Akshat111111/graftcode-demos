{
  "name": "password-strength-checker",
  "version": "1.0.0",
  "description": "A plain JavaScript class that analyses password strength, exposed as a remote service via Graftcode Gateway — no HTTP framework required.",
  "main": "password_checker/checker.js",
  "scripts": {
    "test": "node --test tests/test_password_checker.js",
    "test:verbose": "node --test --reporter=spec tests/test_password_checker.js"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "password",
    "strength",
    "checker",
    "graftcode",
    "security"
  ],
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {}
}
